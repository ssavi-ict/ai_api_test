# 🤖 AI-Driven API Test Framework (Multi-Endpoint Edition)

A scalable **Pytest-based framework** that merges **traditional API validation** with **AI reasoning** for smarter test coverage.

---

## 🚀 Features

- ✅ Supports multiple endpoints (configured in `config.json`)
- 🧠 AI validation of API responses
- 🧩 AI-generated new test ideas
- ⚙️ Configurable test setup
- 🧪 Built on `pytest`
- 🧾 Includes `author.yaml` for project attribution

---

## ⚙️ Setup

```bash
pip install -r requirements.txt
